{
  "name": "election-2028",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "pretest:e2e": "npm run build",
    "test:e2e": "playwright test --grep-invert @career",
    "test:careers": "playwright test --grep @career",
    "test:balance": "node tests/balance.cjs",
    "build": "node scripts/build.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
